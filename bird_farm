# cook your dish here
t=int(input())
for i in range(t):
    x,y,z=map(int,input().split())
    if(z%x==0):
        if(z%y==0):
            print("any")
        else:
            print("chicken")
    elif(z%y==0):
        print("duck")
    else:
        print("none")
